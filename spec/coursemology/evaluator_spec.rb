require 'spec_helper'

RSpec.describe Coursemology::Evaluator do
  it 'has a version number' do
    expect(Coursemology::Evaluator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
